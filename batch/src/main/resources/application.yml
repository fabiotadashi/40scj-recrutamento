spring:
  datasource:
    url: jdbc:h2:~/recrutamentobatch;DB_CLOSE_ON_EXIT=FALSE
    username: fiap
    password: fiap
  batch:
    jdbc:
      initialize-schema: always
      schema: classpath:schema.sql


file:
  path: /Users/fabiomiyasato/Documents/filedelete.txt
  candidatos: import_candidatos.csv